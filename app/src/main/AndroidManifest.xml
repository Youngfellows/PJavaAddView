<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pandora.addview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".JavaAddViewActivity6"></activity>
        <activity android:name=".JavaAddViewActivity5" />
        <activity android:name=".JavaAddViewActivity4" />
        <activity android:name=".JavaAddViewActivity3" />
        <activity android:name=".JavaAddViewActivity2" />
        <activity android:name=".JavaAddViewActivity1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>